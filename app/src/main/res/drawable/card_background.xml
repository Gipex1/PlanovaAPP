<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- Заливка градиентом -->
    <gradient
        android:type="radial"
        android:centerX="50%"
        android:centerY="50%"
        android:gradientRadius="100%"
        android:startColor="#040C0B"
        android:endColor="#10291F"
        android:centerColor="#040C0B" />
    <!-- Обводка (граница) -->
    <stroke
        android:width="2dp"
        android:color="#10291F" />
    <!-- Закругление углов -->
    <corners android:radius="16dp" />
</shape>